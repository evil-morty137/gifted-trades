<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Wallet - GiftedTrades</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Playwrite+HR+Lijeva:wght@100..400&family=Signika+Negative:wght@300..700&family=Ubuntu:ital,wght@0,300;0,400;0,500;0,700;1,300;1,400;1,500;1,700&display=swap"
        rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<style>
    h1,
    p,
    a,
    label {
        font-family: "Signika Negative", sans-serif;
        font-optical-sizing: auto;
        font-weight: 200;
        font-style: normal;
        font-size: 12px;
        color: black;
    }
</style>

<body class="bg-gray-100">
    <!-- Navigation Bar -->
    <%- include('partials/nav-2') %>

        <!-- Main Content -->
        <div class="container mx-auto mt-24 p-8">
            <h1 class="text-3xl font-medium text-center mb-5">Create Wallet</h1>
            <!-- Fund Account Form -->
            <form id="fundForm" class="bg-white shadow-lg rounded-lg p-8 max-w-md mx-auto" method="post"
                action="/create-wallet">
                <div class="mb-4">
                    <label for="name" class="block font-bold mb-2">Name</label>
                    <select id="name" name="name"
                        class="w-full px-3 py-2 border rounded-lg focus:outline-none focus:border-green-500">
                        <!-- <option value="">Send Supported Crypto Currency</option> -->
                        <option value="USDTErc20">Tether (USDTErc20)</option>
                        <option value="TON">TON</option>
                        <option value="BTC">Bitcoin (BTC)</option>
                        <option value="ETH">Ethereum (ETH)</option>
                        <option value="SOL">Solana (Sol)</option>
                        <!-- Add more options as needed -->
                    </select>
                </div>

                <div class="mb-4 mt-6">
                    <label for="address" class="block font-bold mb-2">Address</label>
                    <input type="text" id="address" name="address" placeholder="XXXXXXXXX"
                        class="w-full px-3 py-2 border rounded-lg focus:outline-none focus:border-green-500">
                    <!-- <p id="error-message" class="text-red-500 text-sm mt-2 hidden">Amount must be at least $100.</p> -->
                </div>
                <div class="mb-4 mt-6">
                    <label for="barcode" class="block font-bold mb-2">Barcode</label>
                    <input type="text" id="barcode" name="barcode" placeholder=""
                        class="w-full px-3 py-2 border rounded-lg focus:outline-none focus:border-green-500">
                    <!-- <p id="error-message" class="text-red-500 text-sm mt-2 hidden">Amount must be at least $100.</p> -->
                </div>

                <!-- Proceed Button -->
                <div class="mt-8">
                    <button type="submit" style="background-color: #57B6AD;"
                        class="w-full text-white px-6 py-2 rounded-md transition-colors duration-300 flex items-center justify-center">
                        Submit

                    </button>
                </div>
            </form>
        </div>
</body>

</html>